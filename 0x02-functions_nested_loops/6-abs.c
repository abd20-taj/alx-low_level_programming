#include "main.h"

/**
 * _abs - returns the absolute value of a number
 * @n: paramater
 * Return: the absolute value of the number
 */
int _abs(int n)
{
	if (n < 0)
	{return (n * -1);
	}
	else
	{return (n);
	}
}

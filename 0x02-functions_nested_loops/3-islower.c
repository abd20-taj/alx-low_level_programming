#include "main.h"

/**
 * _islower - checks if the character is in lowercase .
 *
 * Return: 1 if if it is in lower and 0 if it isn't
 */
int _islower(int n)
{

	if (islower(n))
	{
		return (1);
	}
	else
		return (0);
}
